---
import { Play } from "lucide-react";
import { Button } from "@/components/ui/button";
import { getI18N } from "@/i18n";
import { getLangFromUrl, useTranslations } from "@/i18n/utils";
import TagH2 from "@/components/global/TagH2.astro";
import TagH1 from "@/components/global/TagH1.astro"
const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale }) as any;
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const cloudName = import.meta.env.PUBLIC_CLOUDINARY_CLOUD_NAME;
const videoId = import.meta.env.PUBLIC_CLOUDINARY_VIDEO_ID;

const videoUrl = `https://res.cloudinary.com/depikrkrz/video/upload/v1774279594/Video_Web_terwzs.mp4`;
const posterUrl = `https://res.cloudinary.com/${cloudName}/video/upload/f_auto,q_auto/${videoId}.jpg`;
---

<section
  class="relative h-screen min-h-[100vh] w-full overflow-hidden flex items-end justify-center pb-10 md:pb-5"
>
  <video
    autoplay
    loop
    muted
    playsinline
    poster={posterUrl}
    class="absolute z-0 w-full h-full object-cover opacity-0 transition-opacity duration-1000 scale-[1.01]"
    id="hero-video-player"
  >
    <source src={videoUrl} type="video/mp4" />
  </video>

  <div
    class="relative z-20 text-center  text-white px-6 flex flex-col items-center gap-10"
  >
    <div class="flex flex-col gap-4 -mt-85">
      <TagH1 class="font-serif regular">
        {i18n.HOME_PAGE.HERO.TITLE}
        <span class="block not-regular italic">
          {i18n.HOME_PAGE?.SUBTITLE || "company unique"}
        </span>
      </TagH1>
    </div>

    <div class="flex flex-col items-center gap-8 ">
      <Button
        className="-mt-5 bg-[#006083] text-white rounded-none px-12 h-14 uppercase tracking-[0.3em] text-[10px] md:text-xs border border-white/10 transition-all duration-500 shadow-xl"
      >
        {i18n.HOME_PAGE?.HERO?.BUTTON || "Discover Our Journeys"}
      </Button>
    </div>
  </div>

  <div
    class="absolute bottom-6 left-1/2 -translate-x-1/2 z-20 flex flex-col items-center"
  >
    <div
      class="w-[1px] h-12 bg-gradient-to-b from-white/0 via-white to-white/0 opacity-30"
    >
    </div>
  </div>
</section>

<script>
  const video = document.getElementById(
    "hero-video-player",
  ) as HTMLVideoElement;
  if (video) {
    if (video.readyState >= 3) {
      video.classList.remove("opacity-0");
    } else {
      video.oncanplay = () => video.classList.remove("opacity-0");
    }
  }
</script>
